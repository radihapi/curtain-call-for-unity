using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// 道中の敵から、体力のないユニットまで
public class Obstacle : MonoBehaviour
{
    public long lifePoint;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
